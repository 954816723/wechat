  <swiper 
    autoplay="{{autoplay}}" 
    duration="{{duration}}"
    interval="{{interval}}">
    <block 
    wx:for="{{imgUrls}}" 
    wx:key="*this">
    <swiper-item>
        <image src='{{item}}' class='slide-image'/>
    </swiper-item>
    </block>
</swiper>